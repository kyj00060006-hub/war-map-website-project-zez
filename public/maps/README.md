# Maps

Future custom terrain maps and historical basemaps can be placed here.

The first demo uses configurable standard Leaflet tile layers. Later versions can switch individual wars to custom tiles or image overlays without rewriting the main map component.
